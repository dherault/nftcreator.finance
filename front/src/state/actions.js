import { createAction } from '@reduxjs/toolkit'

export const setAccountAddresses = createAction('SET_ACCOUNT_ADDRESSES')
export const foo = createAction('FOO') //  TODO, remove  once there are  2 actions
